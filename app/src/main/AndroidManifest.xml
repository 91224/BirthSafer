<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BirthSafer">
        <activity
            android:name=".SignInPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BloodPressureInputActivity" />
        <activity android:name=".BloodSugarInputActivity" />
        <activity android:name=".SymptomInputActivity" />
        <activity android:name=".SignUpPageActivity" />
        <activity android:name=".MainPageActivity" />
        <activity android:name=".SignUpPage2Activity" />
        <activity android:name=".SignUpPage3Activity" />
        <activity android:name=".CalendarActivity"/>
        <activity android:name=".MyPageActivity"/>
        <activity android:name=".CalendarDetailActivity"/>
        <activity android:name=".ReportActivity" android:exported="false"/>
    </application>

</manifest>